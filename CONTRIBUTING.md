### Contribute

Contributions of new sensor configurations are welcome.

Before submitting, ensure that your sensor configuration has been successfully tested. Testing can be performed by uploading a custom JSON file through the web interface.

After verification, submit a pull request that adds the configuration to the sensor list JSON.
